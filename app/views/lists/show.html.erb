<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.6),rgba(0,0,0,0.6)), url(<%= @list.list_image.attached? ? (cl_image_path @list.list_image.key, height: 800, width: 1200, crop: :fill) : image_path("default-bg.jpg") %>);">
  <div class="container">
    <h1>My <strong><%= @list.name %></strong> list</strong>!</h1>
    <%# <p>Change your life and learn to code at one of our campuses around the world.</p> %>
    <%= link_to "Add a movie", "../lists/#{@list.id}/bookmarks/new", class: "btn btn-primary mt-3" %>
    <%# <a class="btn btn-flat" href="#">Apply now</a> %>
  </div>
</div>
<div class="container mt-3">

 <div class="grid">
    <%# <div id="lists"> %>
      <% @bookmarks.each do |bookmark| %>
          <%= render bookmark %>
      <% end %>

    <%# </div> %>
  </div>
  <%= button_to "Delete List", @list, method: :delete, class: "btn btn-warning text-color-white mt-3" %>
</div>

